server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  client_max_body_size 50m;

  add_header X-Frame-Options "SAMEORIGIN" always;
  add_header X-Content-Type-Options "nosniff" always;
  add_header Referrer-Policy "strict-origin-when-cross-origin" always;

  location = /healthz {
    access_log off;
    add_header Content-Type text/plain;
    return 200 "ok\n";
  }

  location /api/ {
    proxy_pass http://server:3000/api/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 60s;
    proxy_send_timeout 60s;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}

# HTTPS template for a domain-based deployment.
# 1. Put fullchain.pem and privkey.pem under docker/nginx/certs on the host.
# 2. Publish 443:443 in docker-compose.yml.
# 3. Replace example.internal with your real domain, then uncomment this block.
#
# server {
#   listen 443 ssl http2;
#   server_name example.internal;
#
#   ssl_certificate /etc/nginx/certs/fullchain.pem;
#   ssl_certificate_key /etc/nginx/certs/privkey.pem;
#
#   root /usr/share/nginx/html;
#   index index.html;
#   client_max_body_size 50m;
#
#   location = /healthz {
#     access_log off;
#     add_header Content-Type text/plain;
#     return 200 "ok\n";
#   }
#
#   location /api/ {
#     proxy_pass http://server:3000/api/;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto https;
#   }
#
#   location / {
#     try_files $uri $uri/ /index.html;
#   }
# }
